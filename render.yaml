cat > render.yaml << 'EOF'
services:
  - type: web
    name: fatimah-history
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
EOF
